/* import { defineStore } from "pinia"

export const useThemeStore = defineStore("theme", {
    actions: {
        onDarkMode() {
            if (this.darMode) {
                this.darkMode = false
            }

            if (!this.darMode) {
                this.darkMode = true
            }
        }
    },
    state: () => ({ darkMode: false })
})
 */
